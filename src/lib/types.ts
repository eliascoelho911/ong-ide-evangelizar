export type User = {
    id: string
    username: string
    email: string
    firstName: string
    lastName: string
    avatar?: string
}

export type StudentSimpleData = {
    id: string
    name: string
    avatar?: string
}
